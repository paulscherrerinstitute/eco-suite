# Port this box waits on for eco sessions. Installed to
# /etc/eco-control-box.env. No console hostname is needed: the box listens,
# and any machine with the shared token may call it.
LISTEN_PORT=8791
