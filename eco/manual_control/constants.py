"""Shared constants with no eco dependency, so the Pi-side GUI can import
them without dragging in the eco object model."""

MODE_NAVIGATE = "navigate"  # encoder rotate browses the tree
MODE_STEP = "step"  # encoder rotate changes the jog step size
